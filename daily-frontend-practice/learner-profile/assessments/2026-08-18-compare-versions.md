# 2026-08-18：版本号比较复盘

## 证据

- 学习者先完成按 `.` 拆分与逐段比较的总体方向，但初版出现数组长度补齐对象错误、使用原始字符串而非分段数组索引、比较结果值定义不一致等问题。
- 测试指出 `Number` 精度丢失后，学习者采用 `BigInt(arr[i] || "0")` 重写逐段比较，仓库实现已通过包括超出安全整数的测试：[`04-compare-versions.js`](../../days/2026-08-18/04-compare-versions.js)。
- 学习者追问 `1n`、`BigInt("001")` 的含义，正在主动核验类型转换和前导零行为。

## 评估

- 提示级别：三级（已给出 BigInt/字符串比较的关键实现方向）。
- 结论：结果正确，但目前还不能作为“独立稳定掌握”的证据；后续会安排一次不使用 `BigInt` 的字符串数值比较变式。
- 下一步：在滑动窗口题中观察学习者能否先定义状态变量及其不变式，再写循环。
