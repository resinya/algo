# 2026-08-21：无重复字符最长子串（测试通过，待复盘）

## 当前证据

- 学习者独立选择双指针与 `Map`，已写出空字符串提前返回和窗口长度变量。
- 当前卡点：`map.has(s[fast])` 以字符查询，但 `map.set(fast, s[fast])` 以索引作为键，并且 `delete(slow)` 也以索引删除；数据模型的键值职责尚未统一。

## 本轮指导

- 已给一级提示：先统一 `Map` 的键代表什么，再讨论移动窗口。
- 学习者在一级提示后回答 `value` 应存下标，说明已识别 Map 的状态语义；待观察能否在代码中正确处理左边界跳转。
- 学习者随后将 Map 的键统一为字符，并在重复时从左侧删除字符、右侧暂不前进；窗口长度修正为 `fast - slow`。当前发现的最小阻塞点是循环条件允许 `fast === s.length`，会把 `undefined` 当作窗口字符处理。
- 学习者修正循环边界后，2026-09-01 重新运行仓库内全部测试通过。当前实现采用逐个删除窗口左侧字符的方案，正确性已验证；能否独立迁移到另一道滑动窗口题，留待 Day 3 观察。
