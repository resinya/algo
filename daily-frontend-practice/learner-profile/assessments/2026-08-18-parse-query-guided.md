# URL 参数解析：本轮复盘完成，待无提示变式

- 日期：2026-08-18
- 证据来源：对话中粘贴的未完成代码
- 对应题目：Day 1 / 02 `parseQuery`
- 独立程度：首次独立尝试后，在二级提示（规范化步骤骨架）下继续完成
- 测试结果：学习者主动运行测试并在两轮失败后完成修正；老师再次验证，全部测试通过。覆盖完整 URL、纯 query、fragment、flag、百分号解码、`+`、重复键和空输入。

## 指导过程

- 初始思路：学习者已识别 `?`、`&`、`=`、`#`、`+` 和重复键，尝试先分割 URL，再遍历参数并通过 `Map` 聚合。
- 当前卡点：完整 URL、纯 query、fragment、解码、重复键和输出聚合同时交织在一个循环里，导致职责过多。
- 最小提示：先暂停键值解析，只把任意输入规范化为不含 `?` 与 `#` 的纯 query，并生成参数片段数组。
- 学习者反应：第二版能补上 `indexOf('?')` 和“完整 URL 无参数返回空对象”，但混淆了三个切分位置；在三级关键片段提示后，第三版准确完成规范化与数组解构。解码阶段正确写出“先替换 `+`、再调用 `decodeURIComponent`”；收到遮蔽说明后删除同名空函数，把 `return` 移到循环外，并主动运行测试。最后根据 actual/expected 将字符串展开修正为 `[res[key], value]`，全部测试通过。

## 做得好的地方

- 能识别查询字符串的主要分隔符和重复参数需求。
- 主动考虑 fragment、`+` 和重复键，而不只覆盖最简单的单键值场景。
- 已尝试将聚合状态与遍历过程分开表达。
- 第二版已主动删除复杂循环，愿意先聚焦输入规范化。
- 能处理“完整 URL 且不存在 `?`”时返回空对象，也保留了空字符串保护。
- 收到明确反馈后能一次修正全部三个分隔符职责，输入规范化阶段已经清晰。
- 能按要求使用数组解构与默认值，正确覆盖没有 `=` 的 flag 参数。
- 已正确把解码封装成单一职责的 `decode` 辅助函数，并掌握 `+` 替换应先于百分号解码的顺序。
- 能立即删除遮蔽原生 API 的同名函数，并继续推进输出聚合。
- 能主动执行 Node 测试命令，并完整保留 actual、expected 和堆栈信息，开始形成基于反馈定位问题的习惯。
- 重复键聚合的“不存在”和“已有数组”两个分支正确，并已改用 `Object.hasOwn(res, key)` 和动态属性 `res[key]`。
- 能根据 `tag: ['j', 's', 'ts']` 的断言差异定位字符串被按字符展开，并完成最终修正。

## 暴露的问题

- 现象：`split()` 返回数组，却两次使用对象解构 `{ key, value }`、`{ a, b }`，导致变量为 `undefined`；随后调用 `value.includes()` 会报错。
- 根因假设：数组解构 `[]` 与对象解构 `{}` 的使用场景尚不稳定。
- 影响维度：JavaScript 基础与 API。
- 证据强度：普通证据。

- 现象：代码假设输入必定包含 `?`，纯 query 或空输入会在 `splitArr[1].split()` 处报错。
- 根因假设：尚未先统一输入形态，边界处理被混进核心循环。
- 影响维度：问题理解与拆解、边界条件与健壮性。
- 证据强度：普通证据。

- 现象：fragment 在值解析阶段处理，`+` 被拆成数组而不是转换为空格，且尚未调用百分号解码。
- 根因假设：当前对 query 解析的处理顺序不清晰，不代表对应 API 完全不会。
- 影响维度：问题理解与拆解、JavaScript 基础与 API。
- 证据强度：观察。

- 现象：重复键第二次出现时对已有字符串使用展开，可能把字符串拆成字符；函数末尾也尚未返回 `res`。
- 根因假设：聚合状态的类型变化与最终输出流程尚未完成。
- 影响维度：边界条件与健壮性、可读性与模块设计。
- 证据强度：观察。

- 现象：在调用原生 `decodeURIComponent` 的同时又声明同名空函数。由于函数声明提升和作用域查找，`decode()` 调用的是本地空函数，结果为 `undefined`。
- 根因假设：对 JavaScript 全局内置 API 与同名标识符遮蔽不熟悉。
- 影响维度：JavaScript 基础与 API、测试与调试。
- 证据强度：强证据。

- 现象：已经计算 `key/value`，但 `console.log` 仍输出 `rawKey/rawValue`，无法验证解码结果。
- 根因假设：编码后没有让验证目标跟随当前处理阶段更新。
- 影响维度：测试、调试与验证。
- 证据强度：普通证据。

- 现象：`return res` 写在 `for...of` 内部，函数只处理第一个参数，重复键逻辑实际无法执行。
- 根因假设：循环与函数返回的控制流边界没有在编码后进行手动检查。
- 影响维度：JavaScript 基础与 API、测试与调试。
- 证据强度：普通证据。

- 现象：聚合时写成 `[...res.key, value]`。`res.key` 访问固定名称 `key`，不是变量 `key` 指向的动态属性；即使改为 `res[key]`，已有值为字符串时直接展开也会拆成字符。
- 根因假设：动态属性访问和“字符串首次重复后转换为数组”的状态变化不稳定。
- 影响维度：JavaScript 基础与 API、问题理解与拆解。
- 证据强度：普通证据。

## 本次状态变化

- JavaScript 基础与 API：未评估 → 需加强（置信度：中）；本题连续暴露数据形态、字符串方法和全局内置函数遮蔽问题，但能根据明确反馈快速修正。
- 问题理解与拆解：维持发展中（置信度：中）；已完成“输入规范化”独立阶段，后续观察能否继续保持分层处理。
- 边界条件与健壮性：维持发展中（置信度：低）；当前阶段已覆盖完整 URL 无参数、fragment 和空 query。
- 测试、调试与验证：未评估 → 发展中（置信度：中）；虽然日志仍未跟随解码目标，但已主动运行测试并提供完整失败证据。
- 测试、调试与验证补充：能依据 actual/expected 完成第二轮修正并确认通过，正向证据增加；仍需养成删除调试输出和主动检查所有阶段的习惯。
- 已确认当前需加强项为 JavaScript 数据形态、内置 API 和动态属性的准确使用；是否改善需在后续无提示题中验证。

## 口述复盘（第一轮）

- 处理主线：基本正确，能够说明先从 URL 中取得查询部分、去除 fragment、拆参数、拆 key/value、解码和处理 `+`。
- 符号与术语：口述转写中将 `slice` 说成近似 “Sprite”，并把 `&`、`=` 表述成 `@`。可能包含语音转写误差，但面试表达仍需明确说出：`?` 开始 query，`#` 开始 fragment，`&` 分隔参数，`=` 分隔 key/value。
- 百分号解码：理解方向正确；更准确地说，不是“中文无法解析”，而是 URL 中的 Unicode 内容常以 UTF-8 百分号编码表示，需要 `decodeURIComponent` 还原。
- `+` 处理：知道要替换为空格，但尚未回答为何必须在解码前处理，以及 `%2B` 应保留为真正的加号。
- 属性存在性：明确表示不理解为什么不能使用 `if (res[key])`，确认为下一步需要讲解的知识点。
- 复杂度：本轮尚未回答。

## 口述复盘（第二轮）

- 重复空值：理解到结果应为数组，但表述中混入“空数组”。准确结果是 `{ empty: ['', '2'] }`，两个数组元素分别是空字符串和字符串 `'2'`。
- `%2B`：尚不知道结果。已补充说明：当前实现先替换输入中真实存在的 `+`；`%2B` 此时不匹配，随后经 `decodeURIComponent` 还原为真正的加号 `+`。
- 属性存在性：已讲解 `Object.hasOwn` 检查属性本身，而 `if (res[key])` 检查值的 truthy/falsy；空字符串存在但为 falsy。需要后续变式检索确认。
- 复杂度：正确回答时间 `O(n)`、空间 `O(n)`。原因是各阶段处理的总字符数与输入长度线性相关，中间数组和输出最坏也为线性规模。

## 下一步训练

- 删除调试 `console.log`、过期注释和不可达的 TODO 注释，完成代码清理。
- 后续安排字符串解析小变式，验证内置 API 与数据形态能否无提示迁移。
- 在后续检索中复现：`empty=&empty=2`、`plus=%2B`、重复键三种状态和 `Object.hasOwn`。
