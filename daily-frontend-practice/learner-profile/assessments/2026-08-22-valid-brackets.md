# 2026-08-22：有效括号（测试通过，待复盘）

## 当前证据

- 学习者独立写出奇数长度提前返回与数组栈，并尝试为三类括号分别处理。
- 当前卡点：右括号匹配时检查输入的前一个字符 `s[i - 1]`，而不是栈顶的“最后一个尚未匹配左括号”；嵌套合法输入 `([])` 会被误判。
- 另有待修正的条件表达式：`s[i] !== "["` 应与另外两种左括号保持同一语义，当前会把绝大多数字符归入入栈分支。

## 本轮指导

- 已给一级提示：用 `([])` 跟踪栈顶，先建立“栈只保存尚未匹配左括号”的状态不变式。
- 学习者已将三类右括号的匹配对象改为栈顶，并使用无参 `pop()` 移除栈顶，体现出栈操作已纠正。当前只提示修正左括号条件中 `[` 的比较运算符；遍历结束后的非空栈边界待下一步验证。
- 学习者已自行修正 `[` 的条件判断；主循环当前正确实现“左括号入栈、右括号匹配栈顶”。剩余待验证边界：偶数长度但左括号未全部匹配的输入，如 `"(("`。
- 学习者将最终返回值改为 `stack.length === 0`；2026-09-01 重新运行仓库内全部测试通过。栈不变式已在提示后落实，尚需通过间隔变式确认能否独立保持。
