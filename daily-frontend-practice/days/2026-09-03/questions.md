# 2026-09-03：每日四题

> 状态：⬜ 未开始

## 今日要求

- 每题先用中文注释写清：输入输出、核心状态或数据结构、至少一个边界。
- 算法题完成后写时间/空间复杂度；手写题写出一个主动测试的边界。
- 卡住时直接把当前代码贴到对话，我会一次只提示一个关键点。

## 题目清单

1. A：三数之和：返回和为 0 的不重复三元组；排序后使用双指针，跳过重复答案。
2. B：函数柯里化：实现 `curry(fn)`；参数凑够后执行，支持一次传多个参数。
3. C：事件委托：为动态列表设计点击处理函数；用 `closest` 找目标，并说明为什么比逐项绑定更省资源。
4. D：Todo 数据层：实现新增、切换完成、删除、筛选和统计；所有更新必须返回新数组。

对应的题目骨架见本目录的 `01` 至 `04` 文件。

