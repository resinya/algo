# 2026-09-02：每日四题

> 状态：⬜ 未开始

## 今日要求

- 每个题目文件已直接给出中文题意、输入、输出、具体例子和关键边界；编码前先用自己的话复述并画状态变化。
- 算法题完成后写时间/空间复杂度；手写题写出一个主动测试的边界。
- 卡住时直接把当前代码贴到对话，我会一次只提示一个关键点。

## 题目清单

1. A：两数之和变式：返回所有不重复的数值对下标；同一元素不能重复使用，目标 `O(n)`。
2. B：数组扁平化：实现 `flat(array, depth = Infinity)`；跳过稀疏数组空位，不能调用原生 `flat`。
3. C：宏任务与微任务：预测给定 `Promise`、`queueMicrotask`、`setTimeout` 的打印顺序，并写出原因。
4. D：防抖搜索框：实现 `debounce`，要求保留 `this`/参数并提供 `cancel`；用模拟输入验证只执行最后一次。

对应的题目骨架见本目录的 `01` 至 `04` 文件。

