# 2026-09-12：每日四题

> 状态：⬜ 未开始

## 今日要求

- 每个题目文件已直接给出中文题意、输入、输出、具体例子和关键边界；编码前先用自己的话复述并画状态变化。
- 算法题完成后写时间/空间复杂度；手写题写出一个主动测试的边界。
- 卡住时直接把当前代码贴到对话，我会一次只提示一个关键点。

## 题目清单

1. A：搜索旋转排序数组：无重复数字，要求 `O(log n)`；判断哪一半有序。
2. B：手写 `Promise.all`：保持输入顺序、空数组立即 resolve、任一 reject 立即 reject。
3. C：HTTP 缓存：比较强缓存与协商缓存，说明 `Cache-Control`、ETag、304。
4. D：带缓存的请求函数：相同 key 在有效期内复用结果；支持手动失效。

对应的题目骨架见本目录的 `01` 至 `04` 文件。

