# 2026-10-04：每日四题

> 状态：⬜ 未开始

## 今日要求

- 每题先用中文注释写清：输入输出、核心状态或数据结构、至少一个边界。
- 可执行题的文件均内置 `node:assert/strict` 与 `runTests()`；补完实现后直接用 `node 文件名` 验证。
- 浏览器/网络理论题请在对应 Markdown 中完成自测问题。

## 题目清单

1. A：前 K 个高频元素：统计频次后用桶或堆返回前 k 个；处理频次相同时的规则。
2. B：手写 `sleep(ms)` 与超时包装：超时后 reject，不让晚到结果覆盖当前状态。
3. C：AbortController 复习：解释一次取消如何传播到 fetch 和业务状态。
4. D：搜索请求竞态：快速输入时只接受最后一个请求结果，旧请求要取消或忽略。

