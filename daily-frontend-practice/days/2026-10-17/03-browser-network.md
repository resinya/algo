# 浏览器 / 网络 / 异步

## 题目要求

剪贴板 API：处理权限、HTTPS 限制、失败降级。

## 中文解释（已给出）

- 意思：这道题要你分析或完成：剪贴板 API：处理权限、HTTPS 限制、失败降级。
- 输入/场景：待复制文本、HTTPS 页面、clipboard 权限和降级能力。
- 输出/作答：成功/失败结果及使用 Clipboard API 或降级方案的流程。
- 具体例子：await navigator.clipboard.writeText("订单号") 成功后显示提示；拒绝权限时选择文本并提示用户手动复制。
- 关键边界：非安全上下文 API 可能不可用；不能把敏感数据写日志。

## 自测清单

- 用自己的话解释核心概念或 API。
- 给出一个真实项目中的边界或失败场景。
- 说明一个可执行的处理策略、取舍或验证方式。

