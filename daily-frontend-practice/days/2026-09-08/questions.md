# 2026-09-08：每日四题

> 状态：⬜ 未开始

## 今日要求

- 每题先用中文注释写清：输入输出、核心状态或数据结构、至少一个边界。
- 算法题完成后写时间/空间复杂度；手写题写出一个主动测试的边界。
- 卡住时直接把当前代码贴到对话，我会一次只提示一个关键点。

## 题目清单

1. A：反转单链表：迭代实现，返回新头结点；画出 `prev/current/next` 的变化。
2. B：深拷贝基础：支持普通对象、数组、Date、RegExp 与循环引用；用 `WeakMap` 保留共享引用关系。
3. C：Promise 链：手写 `then` 的最小行为说明；解释值透传、返回 Promise 与错误冒泡。
4. D：请求状态机：设计 idle/loading/success/error 四种状态与允许迁移，禁止非法迁移。

对应的题目骨架见本目录的 `01` 至 `04` 文件。

