# 2026-09-24：每日四题

> 状态：⬜ 未开始

## 今日要求

- 每题先用中文注释写清：输入输出、核心状态或数据结构、至少一个边界。
- 可执行题的文件均内置 `node:assert/strict` 与 `runTests()`；补完实现后直接用 `node 文件名` 验证。
- 浏览器/网络理论题请在对应 Markdown 中完成自测问题。

## 题目清单

1. A：零钱兑换：给定硬币和金额，返回最少硬币数；无解返回 `-1`。
2. B：LRU 缓存：用 Map 实现 get/put，访问后更新为最新，容量满时淘汰最旧。
3. C：内存泄漏：列出闭包、定时器、监听器、DOM 引用造成泄漏的例子和清理策略。
4. D：组件资源清理器：统一登记定时器与事件监听器，在 destroy 时全部释放。

