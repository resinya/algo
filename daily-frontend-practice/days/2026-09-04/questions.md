# 2026-09-04：每日四题

> 状态：⬜ 未开始

## 今日要求

- 每题先用中文注释写清：输入输出、核心状态或数据结构、至少一个边界。
- 算法题完成后写时间/空间复杂度；手写题写出一个主动测试的边界。
- 卡住时直接把当前代码贴到对话，我会一次只提示一个关键点。

## 题目清单

1. A：最长回文子串：返回最长连续回文串；使用中心扩散，处理奇偶长度中心。
2. B：手写 `call`：实现 `myCall`，支持普通参数与临时属性清理；说明箭头函数为何不适用。
3. C：URL 与 URLSearchParams：分别用原生 API 解析查询参数、重复键和 hash；比较其与手写解析器的取舍。
4. D：表格排序器：支持单列升降序、数字/中文字符串/空值；不得修改原始列表。

对应的题目骨架见本目录的 `01` 至 `04` 文件。

