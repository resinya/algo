# 2026-09-21：每日四题

> 状态：⬜ 未开始

## 今日要求

- 每题先用中文注释写清：输入输出、核心状态或数据结构、至少一个边界。
- 可执行题的文件均内置 `node:assert/strict` 与 `runTests()`；补完实现后直接用 `node 文件名` 验证。
- 浏览器/网络理论题请在对应 Markdown 中完成自测问题。

## 题目清单

1. A：列表转树变式：父节点缺失时将节点收集到 `orphans`，保持原输入不变。
2. B：URL 参数解析变式：安全处理重复键、空值、`+`、无 `=` 与异常编码。
3. C：Web 安全基础：解释 XSS、CSRF、点击劫持，并各给一个前端防护措施。
4. D：45 分钟模拟：根据路径数组构造路由树；先画一个三层路径的节点复用过程。

