# 2026-09-18：每日四题

> 状态：⬜ 未开始

## 今日要求

- 每题先用中文注释写清：输入输出、核心状态或数据结构、至少一个边界。
- 算法题完成后写时间/空间复杂度；手写题写出一个主动测试的边界。
- 卡住时直接把当前代码贴到对话，我会一次只提示一个关键点。

## 题目清单

1. A：判断平衡二叉树：一次后序遍历计算高度并提前返回，避免重复求高度。
2. B：`Map` 与普通对象选择：实现一个键值缓存，支持任意对象键，并说明为何选 Map。
3. C：DOM 事件阶段：捕获、目标、冒泡；实现可配置 capture 的订阅器。
4. D：可拖拽排序：完成纯数据层的 `moveItem(list, from, to)`，处理越界且不改原数组。

对应的题目骨架见本目录的 `01` 至 `04` 文件。

