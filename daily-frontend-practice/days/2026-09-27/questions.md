# 2026-09-27：每日四题

> 状态：⬜ 未开始

## 今日要求

- 每题先用中文注释写清：输入输出、核心状态或数据结构、至少一个边界。
- 可执行题的文件均内置 `node:assert/strict` 与 `runTests()`；补完实现后直接用 `node 文件名` 验证。
- 浏览器/网络理论题请在对应 Markdown 中完成自测问题。

## 题目清单

1. A：岛屿数量：DFS/BFS 统计网格中的岛屿数；不得越界，明确是否修改网格。
2. B：手写 `EventEmitter` 变式：emit 中监听器自行 off 时，其他监听器仍按预期执行。
3. C：可访问性基础：说明语义化、键盘焦点、aria-label 和对比度。
4. D：Modal 焦点管理：打开后聚焦首个可操作元素，Esc 关闭，关闭后焦点回到触发按钮。

